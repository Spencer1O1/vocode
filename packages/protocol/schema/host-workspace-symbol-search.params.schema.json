{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vocoding.net/schemas/host-workspace-symbol-search.params.schema.json",
  "title": "HostWorkspaceSymbolSearchParams",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "query": {
      "type": "string",
      "description": "Primary symbol or identifier name from the classifier (not a prose phrase). Host still applies case-tolerant matching."
    },
    "symbolKind": {
      "type": "string",
      "description": "Optional classifier hint to prefer LSP symbol kinds: function, method, class, variable, constant, interface, enum, property, field, constructor, module, struct, type. Empty omits filtering."
    }
  },
  "required": ["query"]
}
