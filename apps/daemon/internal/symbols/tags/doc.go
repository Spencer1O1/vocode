// Package tags parses tree-sitter tags CLI output and answers geometric queries (innermost def span).
//
// Package symbols holds SymbolRef, BuildSymbolID, and Resolver; tags is the tree-sitter tag pipeline only.
package tags
